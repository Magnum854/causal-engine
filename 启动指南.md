# 🚀 项目启动指南

## 📍 项目信息

- **项目名称**: 因果推演引擎
- **项目路径**: `X:\因果引擎`
- **架构**: 前后端分离
  - **后端**: FastAPI (Python) - `X:\因果引擎\backend`
  - **前端**: Vite + React - `X:\因果引擎\frontend`

## ⚡ 快速启动（推荐）

### 第一次运行

1. **安装依赖**（只需运行一次）

```bash
# 在项目根目录 X:\因果引擎 下运行
install.bat
```

这会自动：
- ✅ 检查 Python 和 Node.js 环境
- ✅ 安装后端 Python 依赖（FastAPI, Uvicorn, OpenAI 等）
- ✅ 安装前端 Node.js 依赖（React, Vite, TailwindCSS 等）
- ✅ 创建 `.env` 配置文件（DeepSeek API 已配置）

2. **启动项目**

```bash
# 在项目根目录运行
start.bat
```

这会自动：
- ✅ 启动后端服务（http://localhost:8000）
- ✅ 启动前端服务（http://localhost:5173）
- ✅ 自动打开浏览器

## 🔧 手动启动（备选方案）

### 方法 1: 使用脚本

```bash
# 1. 进入项目根目录
cd "X:\因果引擎"

# 2. 首次运行：安装依赖
install.bat

# 3. 启动服务
start.bat
```

### 方法 2: 手动分步启动

**终端 1 - 后端:**

```bash
# 进入后端目录
cd "X:\因果引擎\backend"

# 首次运行：安装依赖
pip install -r requirements.txt

# 启动后端
python main.py
```

**终端 2 - 前端:**

```bash
# 进入前端目录
cd "X:\因果引擎\frontend"

# 首次运行：安装依赖
npm install

# 启动前端
npm run dev
```

## 🌐 访问地址

启动成功后，在浏览器中访问：

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端界面** | http://localhost:5173 | 主应用界面 |
| **后端 API** | http://localhost:8000 | API 服务 |
| **API 文档** | http://localhost:8000/docs | Swagger UI |
| **API 文档** | http://localhost:8000/redoc | ReDoc |

## ✅ 启动成功标志

### 后端启动成功

终端显示：
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 前端启动成功

终端显示：
```
  VITE v5.0.11  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

## 🐛 常见问题排查

### 问题 1: `ModuleNotFoundError: No module named 'fastapi'`

**原因**: Python 依赖未安装

**解决方案**:
```bash
cd "X:\因果引擎\backend"
pip install -r requirements.txt
```

或运行：
```bash
cd "X:\因果引擎"
install.bat
```

### 问题 2: 前端找不到 `package.json`

**原因**: 在错误的目录运行命令

**解决方案**:
```bash
# 确保在前端目录
cd "X:\因果引擎\frontend"
npm install
npm run dev
```

### 问题 3: 端口被占用

**错误**: `Address already in use` 或 `Port 8000 is already in use`

**解决方案**:

```bash
# 查找占用端口的进程
netstat -ano | findstr :8000
netstat -ano | findstr :5173

# 结束进程（替换 <PID> 为实际进程 ID）
taskkill /PID <PID> /F
```

### 问题 4: 中文乱码

**解决方案**: 新版 `start.bat` 已自动设置 UTF-8 编码（`chcp 65001`）

如果仍有问题，手动在终端执行：
```bash
chcp 65001
```

### 问题 5: Python 或 Node.js 未安装

**解决方案**:

- **Python**: 下载安装 Python 3.9+ https://www.python.org/downloads/
- **Node.js**: 下载安装 Node.js 18+ https://nodejs.org/

安装后重启终端。

## 📦 依赖清单

### 后端依赖 (Python)

```
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-dotenv==1.0.0
openai==1.10.0
aiohttp==3.9.1
duckduckgo-search==4.1.0
```

### 前端依赖 (Node.js)

```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "@xyflow/react": "^12.0.4",
  "dagre": "^0.8.5",
  "tailwindcss": "^3.4.1",
  "vite": "^5.0.11"
}
```

## 🎯 快速测试

启动成功后，可以测试以下功能：

1. **基础因果分析**
   - 访问: http://localhost:5173
   - 输入: "全球变暖会导致什么后果？"
   - 查看自动生成的因果关系图谱

2. **标的研究（流式）**
   - 输入标的: "中证1000指数"
   - 观察实时进度反馈
   - 查看三步走 Pipeline

3. **新闻因果提取**
   - 粘贴一段新闻文本
   - 自动提取因果关系
   - 生成智能摘要

## 🛑 停止服务

### 方法 1: 关闭窗口
直接关闭后端和前端的终端窗口

### 方法 2: 使用快捷键
在对应的终端窗口按 `Ctrl + C`

### 方法 3: 结束进程
```bash
# 查找进程
tasklist | findstr python
tasklist | findstr node

# 结束进程
taskkill /IM python.exe /F
taskkill /IM node.exe /F
```

## 📚 相关文档

- **完整 README**: `README.md`
- **API 配置**: `backend/API_CONFIG.md`
- **API 测试**: `backend/API_TEST.md`
- **流式功能**: `backend/STREAMING.md`
- **可视化组件**: `frontend/VISUALIZATION.md`

## 🆘 获取帮助

如果遇到问题：

1. 检查终端输出的错误信息
2. 查看本文档的"常见问题排查"部分
3. 确认 Python 和 Node.js 版本
4. 查看项目 README.md

---

**记住**: 
- 项目根目录: `X:\因果引擎`
- 后端目录: `X:\因果引擎\backend`
- 前端目录: `X:\因果引擎\frontend`
- 首次运行先执行 `install.bat`，然后执行 `start.bat`







